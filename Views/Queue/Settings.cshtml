@{
    ViewData["Title"] = "Queue settings";
}

@model Cua.Models.Queue

<div class="container">
    <div class="row">
        <div class="col-4">
            <div class="separate-div separate-div-no-col">
                <div class="content-div my-content-label-div"><p>Participants:</p></div>
            </div>
            <div class="separate-div separate-div-no-col">
                @foreach (var item in Model.QueueUsers)
                {
                    <div class="content-div my-participant-div">
                        <p>@item.User.Name @item.User.Surname</p>
                        <p class="user-id" hidden>@item.User.Id</p>
                        <a class="my-delete-user-link delete-queue-user" href="#" onclick="return false;"><ion-icon name="close-outline"></ion-icon></a>
                    </div>
                }
            </div>
        </div>
        <div class="col">
            <div class="separate-div">
                <div class="row">
                    <div class="col my-control-input-holder my-control-input-holder-without-padding" style="display: none;">
                        <input id="queue-name-input" type="text" class="my-input" value="@Model.Name" placeholder="Queue name" />
                    </div>
                    <div id="queue-name-info" class="col content-div my-info-div"><p>@Model.Name</p></div>
                </div>
                <div class="row">
                    <div class="col my-control-input-holder my-control-input-holder-without-padding" style="display: none;">
                        <input id="limit-input" type="number" class="my-input" value="@Model.Limit" placeholder="Maximum number of participants" />
                    </div>
                    <div id="limit-info" class="col content-div my-info-div"><p>Limit: @Model.Limit</p></div>
                    <div class="col my-control-input-holder my-control-input-holder-without-padding" style="display: none;">
                        <input id="start-at-input" type="datetime-local" class="my-input" value=@Model.StartAt.ToString("yyyy-MM-ddTHH:mm") />
                    </div>
                    <div id="start-at-info" class="col content-div my-info-div"><p>@Model.StartAt.ToString("dd.MM.yyyy HH:mm")</p></div>
                </div>
            </div>
            <div class="separate-div">
                <div class="row">
                    <div class="col my-show-queues-btn-holder">
                        <button type="button" class="btn btn-primary save-changes save-queue-changes my-qs-btn" style="display: none;">Save changes</button>
                        <button type="button" class="btn btn-primary change-description my-qs-btn">Change description</button>
                    </div>
                    <div class="col my-show-queues-btn-holder">
                        <a asp-action="Control" asp-controller="Room" asp-route-id=@Model.RoomId><button type="button" class="btn btn-primary my-qs-btn">Back</button></a>
                    </div>
        </div>
    </div>
</div>